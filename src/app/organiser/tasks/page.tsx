import { NotBuiltYet } from '@/components/ui/NotBuiltYet';

export default function Page() {
  return (
    <NotBuiltYet
      title="Tasks"
      summary="Task templates: assignment by entitlement or partner, deadlines, links to forms, pages, products or requests, and completion across all partners."
      meanwhile="The ten seeded task templates are in the database and already drive the partner portal."
    />
  );
}
