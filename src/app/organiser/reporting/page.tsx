import { NotBuiltYet } from '@/components/ui/NotBuiltYet';

export default function Page() {
  return (
    <NotBuiltYet
      title="Reporting"
      summary="Partner completion, outstanding and overdue tasks, submission status, orders by partner and supplier, and webhook failures — each with CSV export."
      meanwhile=""
    />
  );
}
