import { NotBuiltYet } from '@/components/ui/NotBuiltYet';

export default function Page() {
  return <NotBuiltYet title="Forms" summary="The forms a partner fills in: all field types, conditional logic, draft saving, submission, organiser feedback and resubmission where allowed." meanwhile="The forms themselves are built and reviewable on the organiser side." />;
}
