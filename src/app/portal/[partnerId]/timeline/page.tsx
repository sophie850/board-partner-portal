import { NotBuiltYet } from '@/components/ui/NotBuiltYet';

export default function Page() {
  return <NotBuiltYet title="Timeline" summary="The partner's tasks and forms plus fixed event milestones on one vertical timeline, with live status." meanwhile="The Key dates content block already gives this treatment inside Information pages." />;
}
